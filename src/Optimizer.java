/**
 * An optimizer is a Solver that is guaranteed to give the optimal solution.
 * Hence, a new inteface is used to distinguish from a basic Solver.
 */
public interface Optimizer extends Solver {
}
